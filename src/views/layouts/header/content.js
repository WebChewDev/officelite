import { content } from "../../assets/content";

export const contents = {
  heading: content[0].home.header.heading,
  subheading: content[0].home.header.subheading,
  cta_btn: content[0].home.header.cta_btn,
};
