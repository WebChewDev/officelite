import { content } from "../../assets/content";

const card = content[0].home.features.pricing_cards;

export const heading = card.heading;
export const pricing = card.pricing;
export const subheading = card.subheading;
export const subtext1 = card.subtext_1;
export const subtext2 = card.subtext_2;
export const subtext3 = card.subtext_3;
export const btn = card.btn;
