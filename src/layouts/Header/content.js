import { content } from "../../assets/content";

export const heading = content[0].home.header.heading;
export const subheading = content[0].home.header.subheading;
export const cta_btn = content[0].home.header.cta_btn
